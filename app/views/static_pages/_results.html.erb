<hr>

<div class="alert alert-success">
  <h3>Exact match</h3>
</div>

<% if !@exact_match.empty? %>
  <ul>
    <li>
      <div class="show_ontology" term_name="<%= @exact_match[0] %>">
      <h4><%= link_to @exact_match[0], ontology_path(:term_name => @exact_match[0]) %></h4>
      </div>
    </li>
  </ul>
<% else %>
  <p class="lead text-error text-center">Nothing Found</p>
<% end %>

<hr>

<div class="alert alert-block">
  <h3>Other hits</h3>
</div>

<% if !@other_hits.empty? %>
  <%= render 'anchor_letter' %>

  <ul>
    <% @other_hits.each do |keyword| %>
      <li>
        <% if @anchors[keyword[0].upcase] == 0 %>
          <div class="show_ontology" term_name="<%= keyword %>">
          <h4><%= link_to keyword, ontology_path(:term_name => keyword), :"term_name" => keyword, :name => keyword[0].upcase %></h4>
          </div>
          <% @anchors[keyword[0].upcase] = 1 %>
        <% else %>
          <div class="show_ontology" term_name="<%= keyword %>">
          <h4><%= link_to keyword, ontology_path(:term_name => keyword), :"term_name" => keyword %></h4>
          </div>
        <% end %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p class="lead text-error text-center">Nothing Found</p>
<% end %>

